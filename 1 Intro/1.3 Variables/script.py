name = input("whats your name ") # Store input in a variable
length = len(name) # Get length of the input string
print("Hello " + name + "! Your name is " + str(length) + " characters long.") # Concatenate strings with + operator